<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guo.bean.mapper.AccountMapper">
    <select id="getAccountById" resultType="com.guo.bean.Account">
        select *
        from novelproject.account
        where user_id = #{id}
    </select>
    <select id="getPasswordById" resultType="String">
        select user_password
        from novelproject.account
        where user_id = #{id}
    </select>
    <insert id="insertUser">
        insert into novelproject.account (user_id, user_name, user_password)
        values (#{id}, #{name}, #{password})
    </insert>
    <select id="getSalt" resultType="String">
        select salt from novelproject.account where user_id = #{id}
    </select>
</mapper>